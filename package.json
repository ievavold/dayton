{
  "name": "dayton",
  "version": "0.1.0",
  "private": true,
  "description": "a stylesheet",
  "scripts": {
    "publish": "rm -rf dist && parcel build -d dist --public-url '/dayton' demo/index.html && gh-pages -d dist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ievavold/dayton"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.5",
    "sass": "^1.32.8"
  }
}
